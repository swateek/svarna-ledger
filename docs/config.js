// From Supabase Dashboard → Project Settings → API
// Publishable key (sb_publishable_...) — safe to commit when RLS only allows SELECT.
window.SUPABASE_URL = 'https://xxxx.supabase.co';
window.SUPABASE_PUBLISHABLE_KEY = '';
